plugins {
    id 'java'
    id 'io.freefair.lombok' version '8.6'
}

group = 'org.jobhunter'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

ext{
    springWebVersion = '6.1.5'
}

dependencies {
    implementation(
            "org.aeonbits.owner:owner-java8:${project.ext.ownerVersion}"
            , "org.springframework:spring-web:${springWebVersion}"
    )
}

test {
    useJUnitPlatform()
}